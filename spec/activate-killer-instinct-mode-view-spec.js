'use babel';

import ActivateKillerInstinctModeView from '../lib/activate-killer-instinct-mode-view';

describe('ActivateKillerInstinctModeView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
